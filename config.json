{
	"email": {
		"enabled": false,
		"host": "smtp.host",
		"port": 465,
		"secure": true,
		"from": "You <you@yourdomain>",
		"user": "you@yourdomain",
		"pass": "password",
		"subject": "Your new password",
		"text": "Hi,\n\nYour new password is:\n\n{{password}}\n",
		"html": "<p>Hi,</p><p>Your new password is:</p><p><strong>{{password}}</strong></p>"
	},
	"cacheSize": 5,
	"headers": {
		"cache-control": "private, no-cache, must-revalidate"
	},
	"logging": {
		"level": "info",
		"enabled": true
	},
	"instruction": {
		"create": "POST to generate a password. Optional parameters `words` (3) to specify amount to use, `min` to set the minimum length, `max` to set the maximum length, `email` to send as an Email, `passwords` (1) to generate a list, & `special` (false) to enable common special characters.",
		"error": "Could not generate a password that met criteria in a reasonable amount of attempts.",
		"invalid": "Invalid arguments"
	},
	"attempts": 10,
	"max": 100,
	"port": 9090,
	"title": "cocoa"
}